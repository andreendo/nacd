const crypto = require('crypto');

// console.log(Object.getOwnPropertyDescriptor(crypto, 'createCipher'));

// Object.defineProperty(crypto, 'createCipher', () => { console.log('my Cipher'); });
// crypto.createCipher();
